{
  "compilerOptions": {
    "target": "es2020",
    "watch": true,
    "moduleResolution": "node",
    "lib": ["dom", "es2020"], // adds external libraries like Classes from  the DOM
    "outDir": "dist",
    "rootDir": "src",
    "strict": true,
  }
}
